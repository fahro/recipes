# Recipe App API
Recipe app api source code.


Run project locally:
```
 docker-compose up
```
Open: `localhost:8000`

Run tests command:
```
 docker-compose run app sh -c "python manage.py test&&flake8"
```
